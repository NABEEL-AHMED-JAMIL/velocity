{
   "bid": {
            #if($campaign.getIabs())
              "iabs" : "$campaign.getIabs()"
              #if($campaign.getAdId() || $campaign.getCampaignId()),#end
            #end
 	   	    #if($campaign.getCampaignId())
               "campaignId" : "$campaign.getCampaignId()"
               #if($campaign.getAdId()),#end
 	   	    #end
 	   	    #if($campaign.getAdId())
 	   	       "adId" : "$campaign.getAdId()"
 	   	    #end
 	   	 }
}